# A-Share Dpoint ML Trading System - Requirements
# ================================================
# 安装依赖 (Install dependencies):
#   pip install -r requirements.txt
#
# 开发与测试 (Development & Testing):
#   pip install -r requirements.txt pytest
#
# 深度学习升级版 (Deep Learning Upgrade - Optional):
#   pip install optuna pyarrow h5py matplotlib
# ================================================

# =========================================================
# Core dependencies (核心依赖)
# =========================================================
# 使用范围版本以允许兼容更新，同时保证最低版本要求
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
joblib>=1.3.0,<2.0.0
openpyxl>=3.1.0,<4.0.0
xlsxwriter>=3.1.0,<4.0.0

# PyTorch - 根据 CUDA 版本选择安装
# CPU 版本：pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA 11.8: pip install torch --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0,<3.0.0

xgboost>=2.0.0,<4.0.0
tabulate>=0.9.0,<1.0.0

# =========================================================
# Deep Learning Upgrade (深度学习升级 - 可选)
# =========================================================
# 以下依赖用于增强功能，可根据需要安装
optuna>=4.0.0,<5.0.0        # 超参数搜索可视化
pyarrow>=15.0.0,<20.0.0     # 高性能数据读取
h5py>=3.10.0,<4.0.0         # HDF5 模型存储
matplotlib>=3.8.0,<4.0.0    # 可视化绘图

# =========================================================
# Testing (测试工具)
# =========================================================
pytest>=8.0.0,<9.0.0

# =========================================================
# Optional - Visualization (可选 - 可视化工具)
# =========================================================
# plotly>=5.18.0,<6.0.0      # Optuna 可视化（可选）
# kaleido>=0.2.1             # Plotly 图片导出（可选）
# tensorboard>=2.15.0,<3.0.0 # 训练可视化（可选）
# shap>=0.44.0,<1.0.0        # 特征解释（可选）

# =========================================================
# Optional - Performance (可选 - 性能工具)
# =========================================================
# accelerate>=0.25.0,<1.0.0  # Hugging Face 加速库（可选）
# einops>=0.7.0,<1.0.0       # 张量操作库（可选，用于某些注意力实现）

# =========================================================
# Development (开发工具 - 可选)
# =========================================================
# black>=23.0.0              # 代码格式化
# flake8>=6.0.0              # 代码检查
# mypy>=1.0.0                # 类型检查
# isort>=5.12.0              # 导入排序
